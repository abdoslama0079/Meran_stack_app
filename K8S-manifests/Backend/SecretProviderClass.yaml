apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: mern-kv-provider
  namespace: mern-prod
spec:
  provider: azure
  parameters:
    usePodIdentity: "false"
    useVMManagedIdentity: "false"          # 🔹 Changed to false
    useWorkloadIdentity: "true"            # 🔹 ADDED/Set to true
    keyvaultName: "akskv003883223"
    tenantId: ""
    clientID: "" # Ensure this is the ID for aks-kv-identity
    objects: |
      array:
        - |
          objectName: mongo-root-username
          objectType: secret
          objectAlias: username
        - |
          objectName: mongo-root-password
          objectType: secret
          objectAlias: password
  secretObjects:
    - secretName: mongo-sec
      type: Opaque
      data:
        - objectName: username
          key: username
        - objectName: password
          key: password
